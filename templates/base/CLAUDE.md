# [Project Name]

This base file is overlaid by the selected Meridian workflow. Read
`PROJECT_WORKFLOW.md` and the local `LANGUAGE_POLICY.md` before acting; the
workflow file selects either `LEAN_DELIVERY` or `GOVERNED_SDD` and is the source
of operational rules.

## Commands

```bash
# Fill in the project's run, test, lint, and format commands.
```
