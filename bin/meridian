#!/usr/bin/env python3
"""Run the Meridian framework maintenance CLI from a source checkout."""

from pathlib import Path
import runpy


runpy.run_path(
    str(Path(__file__).resolve().parents[1] / "scripts" / "meridian.py"),
    run_name="__main__",
)
